server:
  host: "0.0.0.0"
  port: 8000
  log_level: "info"

hardware:
  device: "auto"        # auto | cuda | mps | cpu
  vram_gb: 11           # 0 если GPU нет
  ram_gb: 30
  cpu_threads: "auto"   # auto = os.cpu_count()

whisper:
  engine: "faster"      # faster | whispercpp

  ggml_repo: "ggerganov/whisper.cpp"
  ggml_model: "ggml-large-v3.bin"
  ggml_path: "/models/ggml-large-v3.bin"

  hf_model: "bond005/whisper-podlodka-turbo"
  use_hf_model: false

  fallback_model: "medium"   # tiny | base | small | medium | large-v3
  device: "cpu"              # auto | cuda | cpu
  compute_type: "auto"       # auto | int8 | int8_float32 | float16 | float32
  beam_size: 5

llm:
  enabled: true
  repo: "bartowski/Qwen2.5-7B-Instruct-GGUF"
  filename: "Qwen2.5-7B-Instruct-Q4_K_M.gguf"
  local_path: "/models/qwen/Qwen2.5-7B-Instruct-Q4_K_M.gguf"

diarization:
  enabled: true

# null = считается автоматически из hardware
overrides:
  llm_gpu_layers: null   # -1 все слои на GPU, 0 только CPU
  llm_context: 16384
  llm_threads: null
